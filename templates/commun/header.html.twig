<header class="{% if is_granted('ROLE_ADMIN') %}bg-danger{% elseif is_granted('ROLE_MEMBRE') %}bg-success{% else %}bg-primary{% endif %} mb-5">
	<nav class="navbar navbar-expand navbar-dark container px-3">
		<a class="navbar-brand" href="{{ path('home') }}">AGENCELOC</a>
		<ul class="navbar-nav ms-auto">
			<li class="nav-item"><a class="nav-link" href="{{ path('home') }}">Accueil</a></li>
			{% if is_granted('ROLE_ADMIN') %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="dropGestion" role="button" data-bs-toggle="dropdown" aria-expanded="false">Gestion</a>
					<ul class="dropdown-menu" aria-labelledby="dropGestion">
						<li><a class="dropdown-item" href="{{ path('commande_index') }}">Commandes</a></li>
						<li><a class="dropdown-item" href="{{ path('membre_index') }}">Membres</a></li>
						<li><a class="dropdown-item" href="{{ path('vehicule_index') }}">Vehicules</a></li>
					</ul>
				</li>
			{% endif %}

			{% if app.user %}
				<li class="nav-item"><a class="nav-link" href="{{ path('logout') }}">Déconnexion</a></li>
			{% else %}
				<li class="nav-item"><a class="nav-link" href="{{ path('login') }}">Connexion</a></li>
			{% endif %}
		</ul>
	</nav>
</header>
